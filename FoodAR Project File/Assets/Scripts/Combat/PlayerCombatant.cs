using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerCombatant : Combatant
{

    /*public override void UseAttack(Combatant p_target)
    {
        base.UseAttack(p_target);

        ActorDataManager targetDataManager = p_target.GetComponent<ActorDataManager>();

        targetDataManager.ModifyHealth(-this.GetComponent<ActorDataManager>().NormalDamage);
        AddSpecialPoints
    }

    public override void UseDefense(Combatant p_target)
    {
        base.UseDefense(p_target);


    }

    public override void UseSpecial(Combatant p_target)
    {
        

        base.UseSpecial(p_target);
    }*/
}
